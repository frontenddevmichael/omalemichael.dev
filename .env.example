# This file is safe to commit -- it's a template, not real secrets.
# The actual GitHub token belongs SERVER-SIDE ONLY (e.g. Vercel project
# environment variables), named GITHUB_TOKEN (no VITE_ prefix).
# Do not create a VITE_GITHUB_TOKEN anywhere -- Vite ships VITE_* vars
# straight into the public browser bundle.
